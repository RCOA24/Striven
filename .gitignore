# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (CRITICAL - Never commit these!)
.env
.env.*
*.env
.env.local
.env.development
.env.production
.env.test

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ensure PWA files are tracked (not ignored)
!public/sw.js
!public/manifest.json


# ----------------------------------
# Capacitor / Android Native Project
# ----------------------------------

# Android generated build files (DO NOT COMMIT THESE)
android/build/
android/app/build/
android/.gradle/
android/app/release/
android/app/debug/

# Local configuration file (contains your local SDK path)
local.properties
android/local.properties

# Android Studio settings
android/.idea/
android/app/*.iml
android/*.iml
.gradle/

# Keystore files (CRITICAL - Contains signing keys!)
*.jks
*.keystore
*.p12
*.key
release.keystore
debug.keystore

# Generated files
android/app/src/main/assets/public/
android/app/src/main/res/

# Capacitor specific (usually inside android/app/src/main/assets)
# Depending on your setup, you might want to ignore the web assets copied over
# But usually, Capacitor manages this. 
# Ideally, don't ignore 'public' inside android, let Capacitor handle sync.